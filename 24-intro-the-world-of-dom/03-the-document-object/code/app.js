// View the properties of the "document" object
console.dir(document);

// View the ALL of the HTML elements in the web page as JavaScript objects in an array
console.log(document.all);

// To change the text of the 1st HTML element <b>
document.all[10].innerText = "Sealkey";
