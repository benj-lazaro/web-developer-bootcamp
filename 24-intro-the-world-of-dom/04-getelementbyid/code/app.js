// Select the DOM object w/ an "id" attribute of "banner"
const banner = document.getElementById("banner");

// View the selected DOM object's HTML representation
console.log(banner);

// View the selected DOM object's properties
console.dir(banner);

// Select the DOM object w/ an "id" attribute of "toc"
const toc = document.getElementById("toc");
console.log(toc);
console.dir(toc);
